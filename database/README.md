# Database

The project uses SQLite for the simple local/demo architecture.

Run `schema.sql` against `password_manager.db` before starting the backend.

For GitHub, the empty database file is intentionally not included. The application should initialize the schema before first use.
